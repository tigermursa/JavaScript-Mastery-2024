//normal Object :

const thisArray = {
  0: 4,
  1: 6,
  2: 8,
  3: 10,
};

//normal Array :
const nowLookLike = [4, 6, 8, 10];

console.log(thisArray[0]);
console.log(nowLookLike[0]);

// So in JavaScript, arrays are a special type of object that is used to store an ordered collection of values.
//An array in JavaScript is an ordered, indexed collection of values that can include various data types.

const holyArray = ["Name", 45, true, undefined, null];
console.log(holyArray[4]);  //so array can store these values , but should store
